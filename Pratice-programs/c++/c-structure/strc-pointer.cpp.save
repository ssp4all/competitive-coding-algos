#include<iostream>
using namespace std;
int main()
{
    typedef struct student{
        int roll;
        char name[100];
    };

    student stud, *pstud;
    pstud = &stud;
    cout<<"Enter Roll no. and name:";
    cin>>(pstud->roll);
    cin>>(pstud->name);
    
    cout<<(pstud->name);
    


    return 0;
}
